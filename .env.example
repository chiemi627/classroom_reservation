PUBLIC_CALENDAR_URL=https://example.com/your-calendar.ics
# Optionally protect the refresh endpoint. Generate a long token and set it in your deployment env / GitHub Secrets.
CALENDAR_REFRESH_TOKEN=your-generated-token-here
